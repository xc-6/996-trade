export const defaultFilter: Record<string, { min?: number; max?: number }> = {
  unsoldAmount: {
    min: 0.000001,
    max: 1000000,
  },
  totalUnsoldAmount: {
    min: 0.000001,
    max: 1000000,
  },
};
